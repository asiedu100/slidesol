{
  "name": "slidesol",
  "private": true,
  "type": "module",
  "description": "No build step for the site itself — this manifest exists only to run the test suite (node --test, zero dependencies).",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
